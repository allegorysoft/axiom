<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>$(DotnetVersion)</TargetFramework>
        <RootNamespace/>
    </PropertyGroup>

    <!--
        Namespace convention: `Extensions` and `Options` folders stay in the
        root namespace consumers need them without extra usings.
        Other folders (Repositories, Interceptors, etc.) get a namespace
        suffix matching the folder name.
    -->

    <ItemGroup>
        <PackageReference Include="Microsoft.EntityFrameworkCore"/>
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\..\ddd\Allegory.Axiom.Domain\Allegory.Axiom.Domain.csproj"/>
    </ItemGroup>

</Project>
